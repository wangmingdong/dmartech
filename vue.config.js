// vue.config.js
module.exports = {
    // lintOnSave: false,
    // devServer: {
    // host: 'localhost', //域名
    // port: 8080, //端口
    // proxy: 'http://61.147.77.40:80'
    // proxy: { //进行代理转发
    //     '/api': {
    //         //要解决跨域的接口的域名
    //         target: 'http://dmdelivery.com/webservice/',
    //         // 是否开启本地代理 默认true; 如果接口跨域，需要进行此参数配置
    //         changeOrigin: true,
    //         //如果是https接口，需要配置这个参数
    //         secure: false,
    //         pathRewrite: {
    //             '/api': '' //重写地址
    //         }
    //     }
    // }
    // open: false,
    // host: '127.0.0.1',
    // port: 8080,
    // '/api': {
    //     target: 'http://dmdelivery.com/webservice/', // 需要代理的后端接口
    //     changeOrigin: true, //开启代理：在本地会创建一个虚拟服务端，然后发送请求的数据，并同时接收请求
    //     pathRewrite: { //重写匹配的字段，如果不需要在请求路径上，重写为""
    //         '/api': ''
    //     }
    // }
    // }
}